
  # Ruby on Rails Tutorial and Questions

  This is a Rails-focused quiz learning platform that renders interactive question sets from structured JSON.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
